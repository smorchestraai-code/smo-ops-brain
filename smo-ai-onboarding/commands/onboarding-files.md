---
description: "Manual request and process customer files (1d)."
---

# /onboarding-files

Manual trigger for Sub-step 1.D (files request + processing).

Invokes the `1d-files-requester` skill. Stage A generates customized request list per persona. Stage B extracts text from customer-provided files and classifies signals.

See `phases/phase-1-input-capture-brd.md` Section 5.4 and `skills/1d-files-requester/SKILL.md`.
